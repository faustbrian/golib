# Specification decisions

The canonical append-only decision register moved to
[`docs/specification-decisions.md`](../docs/specification-decisions.md). The
stable identifiers `XSD-DEC-001` through `XSD-DEC-004` are preserved there and
the register now covers the complete observable specification policy. This
compatibility pointer remains so existing provenance links do not break.
