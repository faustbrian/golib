version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - exhaustive
    - gosec
    - misspell
    - nilerr
    - prealloc
    - revive
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports
