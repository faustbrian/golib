# Security policy

Report vulnerabilities privately. Never attach credentials, signed requests,
sources, queries, cursors, PIT/task IDs, authorization errors, or backend bodies
to a public report. State the adapter and OpenSearch versions and provide only
sanitized reproduction metadata.
