# Contributing

Use a conventional branch and commit message. Add a failing behavior test
before production changes, keep all carrier input bounded, and do not weaken
trust, privacy, or semantic separation. Run `make check-all` before review.
Public API changes must update the API baseline and changelog.
