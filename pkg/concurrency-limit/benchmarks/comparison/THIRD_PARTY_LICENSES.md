# Third-party licenses

Failsafe-Go v0.9.6 is used under MIT. Platinum
`go-concurrency-limits` v1.0.0 and the pinned Netflix Gradient2 source are used
under Apache-2.0. Exact Go dependency versions and transitive checksums are
recorded in `go.mod` and `go.sum`.

The Netflix reference equation is a translated derivative used only by this
non-releasable evidence module. Its source revision, file checksum, and update
procedure are recorded in `PROVENANCE.md`.
