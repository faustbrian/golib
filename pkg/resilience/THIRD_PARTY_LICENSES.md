# Third-party licenses

The production package uses only the Go standard library. Benchmarks use:

- `github.com/failsafe-go/failsafe-go` v0.9.6 - MIT License.

Its transitive benchmark-only module graph is recorded by `go.mod` and
`go.sum`. Repository release gates generate the complete compiled dependency
license inventory and software bill of materials.
