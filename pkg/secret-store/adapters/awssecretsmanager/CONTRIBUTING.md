# Contributing

Preserve immutable version semantics, caller-owned deterministic identity,
unique staging labels, least-privilege AWS operations, bounded inputs,
secret-safe diagnostics, and exact production statement coverage.

Run the module's applicable repository gates before delivery. Mutation testing
remains a release gate and must not be replaced by line coverage.
