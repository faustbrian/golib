# Contributing

Run `make check` before submitting changes. Start behavior changes with a
failing test. Provider changes must pass shared conformance and real-service
integration. Stable IDs, codec versions, export formats, and schema are
compatibility contracts. Never include values or credentials in diagnostics.
