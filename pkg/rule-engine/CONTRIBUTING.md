# Contributing

Open an issue before changing public semantics. Add a focused failing test,
implement the smallest behavior, and run `make check`. New operators require a
complete truth table, JSON grammar coverage, limits, fuzz seeds, mutation
coverage, documentation, and compatibility analysis.

Keep commits conventional and explain why the change is necessary.
