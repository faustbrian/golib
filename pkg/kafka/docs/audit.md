# Pre-v1 implementation audit

This audit records the starting point and current disposition of the
production-policy redesign. A passing check against the draft is not evidence
that the draft has the right contract. Status describes executable behavior in
the current tree through 2026-08-02, not intended behavior.

## Current implementation inventory

| Area | Current executable behavior | Required disposition |
| --- | --- | --- |
| Configuration | Separate producer, consumer, replay, and inspector structs validate identities, durations, byte limits, and Kafka broker-compatible topic names. Producers require a copied bounded topic allowlist; consumers, replay, and inspectors have explicit topic or group target sets. A shared owned protocol policy preserves `ApiVersions` negotiation and optionally applies a validated minimum request-version floor. Security configuration owns explicit transport/authentication policy, redacted formatting, and defensive copies. | Add optional reviewed resolver policies and complete incompatible-option validation across every concern. |
| Records | Stable producer and consumed-record models expose explicit automatic or exact partition selection, timestamp type, and leader epoch, copy producer input, and provide `Retain` for borrowed consumed bytes. | Bound diagnostic copies separately from transport limits. |
| Producer | Synchronous single, synchronous batch, and bounded asynchronous methods return per-record delivery metadata. Batch results are restored to input order by owned record identity because franz-go callback completion can differ across partitions. Keyed production is the safe default; exact partitions require an explicit record policy; detected idempotent-producer data loss stops the producer; delivery errors include redacted fatal and ambiguous classifications, including retryable broker-restart dial failures; drain, abort, shutdown, and error-returning bounded close preserve preexisting admissions. Producer admission has independent record-count and byte limits, and the byte limit cannot be smaller than one allowed batch. Retry uses explicit exponential, jittered bounds while failed-partition metadata refresh retains a separate 250-millisecond safety floor. Non-transactional idempotent production permits bounded in-flight cancellation and classifies delivery timeout or exhausted retries as ambiguous because a broker acknowledgement may have been lost. Transactional production instead closes and fatally fences the complete owned client when an admitted record exceeds its delivery bound because per-record cancellation is incompatible with transactional IDs. Broker throttling is surfaced separately as request-level observer metadata because one produce request can contain many records and franz-go does not expose an authoritative record-to-throttle mapping. The pinned single-broker fixture proves ordered batch delivery, asynchronous delivery, graceful shutdown draining an admitted record, post-shutdown fencing, exact broker-visible values, and successful delivery under a broker-enforced client quota with a positive post-response throttle event. The three-broker Apache fixture proves a two-topic partial batch where one record is acknowledged and persisted while the other is rejected by `max.message.bytes`, with exact input-ordered success and `ErrorOversized` attribution. It also proves acks-all explicit-partition synchronous delivery before failure, with RF=3/ISR=2 after leader failover, after ISR recovery, after every matching non-transactional `Produce` response is lost, and after transactional `Produce` response loss for both producer-only and consume-transform-produce clients. The transactional faults return bounded ambiguous fatal outcomes, reject reuse, keep outputs read-committed invisible, and preserve the processor source offset. Separate secured Apache fixtures prove producer delivery through mTLS, PLAIN, both SCRAM mechanisms, and signed-JWT OAUTHBEARER. A nested harness compares equivalent idempotent all-ISR synchronous single-record, 10/100-record batch, bounded 10/100-record asynchronous-window, keyed plus explicit eight-partition, and producer transaction contracts with raw franz-go and Sarama across keying, payload, and compression variants; kafka-go remains outside those rankings because it lacks idempotent production or transactions. | Add reconnect and resource evidence. |
| Transactions | A producer callback can begin, synchronously produce, commit, or abort. A separate processor owns one read-committed group member and transactional producer, treats one bounded source poll as one all-or-nothing transaction, commits source offsets only with successful Kafka outputs, fences callback lifetime and unsafe lifecycle failures, and closes through a bounded retryable lifecycle. The single-broker fixture proves producer visibility, aborted-source filtering, atomic source-offset/output commit, abort, and redelivery. A pinned three-node Apache Kafka 3.7.2 fixture establishes the minimum supported broker transaction path with exact runtime assertion, RF=3, `min.insync.replicas=2`, producer commit/abort visibility, processor source-offset/output atomicity, abort invisibility, redelivery, and retry. Current 4.3.1 evidence additionally proves broker recovery at ISR two, same-transactional-ID fencing, process-termination recovery, eager and cooperative rebalance recovery, broker-enforced expiry, committed `EndTxn` response loss, and bounded ambiguous transactional `Produce` response loss. The nested real-broker harness separately compares one-record and ten-record producer transactions against raw franz-go and Sarama, plus atomic consume-transform-produce against raw franz-go. | Add minimum-version broker-failure, fencing, timeout, response-loss, and rebalance evidence. |
| Consumer groups | A classic group explicitly selects cooperative-sticky, eager-sticky, or an eager-to-cooperative rollout policy; optional static membership and rack identity are validated. It bounds concurrent fetches, minimum, aggregate, and per-partition fetch bytes, hard encoded broker responses, individual decoded batches, and active decoded buffers, and rejects fetched records outside explicit record limits before the package copies header metadata, and uses an explicit fixed-size worker limit across independent partitions while keeping each partition sequential. Record handling stops each partition at its first failure and commits only contiguous successful prefixes; batch handling is all-or-nothing per partition. Rebalances are blocked during the poll; a fast blocked callback stops admission and either requests cooperative cancellation of every active handler or drains only handlers already active before settlement. Bounded internal callbacks track incremental assignments, revocations, fatal loss, and a package-local settlement epoch without exposing franz-go or claiming Kafka's broker generation ID. Explicit subscribed partitions can be paused and resumed within a bounded accumulated set. Public drain interrupts an idle poll without canceling admitted handlers, preserves their settlement, fences new work after an incomplete wait, and composes with graceful shutdown before group leave and close. A duplicate static instance ID creates a terminal consumer lifecycle state rather than an automatic rejoin loop, and the single-broker fixture proves both the broker fence and rejection before a later poll. The same fixture proves an eager join cancels one active handler under `RebalanceCancelHandler` without settling it and redelivers the source record, while `RebalanceDrainHandler` lets one active handler finish and commit before releasing the blocked rebalance so the joining member begins at the next offset. A three-broker, two-process fixture extends both outcomes across two active partitions: cancel commits neither and redelivers both offset-zero records, while drain commits both offset-one positions before the replacement handles the next records. Per-record and whole-batch decorators add bounded category-selected retry, versioned retry-topic and dead-letter publication, stop, and application delegation. Whole-batch policy validates and owns one ordered partition batch, retries or reroutes every record, resolves only after exact definite target results, and exposes partial delivery results without committing the source batch. The broker fixture proves per-record publish-then-commit and failed-publication redelivery plus whole-batch reroute-then-commit with preserved source coordinates and batch positions. It also proves that an accepted compressed Kafka record batch whose decoded records exceed policy reaches no consumer, transaction, or replay handler and settles no source offset. Reclaimable active-buffer accounting is limited to record batches (magic 2); legacy compressed message sets are explicitly unsupported because franz-go cannot return their decoded allocation through record recycling. A separate three-broker fixture uses eager-only, migration, and cooperative-only child processes to prove the documented two-phase protocol transition and exact one-copy ownership of both partitions across stable membership replacement. The same rack-configured fixture proves that a separate consumer process configured for a non-leader replica's rack handles and commits a source record after its single in-flight fetch completes on that follower. A nested client harness compares one-record and 10/100-record application-batch handling followed by manual commits for the policy, raw franz-go, kafka-go, and Sarama; independent checks prove exact handler order and final broker offsets before the healthy-broker timing capture. Pinned three-broker Kafka 4.3.1 fixtures also prove a cooperative join revokes exactly one partition after both in-flight partitions drain and commit, then preserves disjoint retained and replacement ownership; administrative removal of an active static member rejects its stale-generation commit with `UNKNOWN_MEMBER_ID`, reports one lost partition, clears local assignment, and redelivers the unsettled offset; and process suspension beyond the six-second broker session timeout transfers the same in-flight record to a replacement before the resumed stale member is rejected with `UNKNOWN_MEMBER_ID` and reports ownership loss. | Add repeated cooperative churn and broader minimum-version rebalance evidence. |
| Replay | Explicit partition offsets are directly assigned without a group. Local and broker-validated owned plans apply external checkpoints; exact-partition timestamp-window planning rejects retention-ambiguous starts and produces owned executable ranges. Handler execution requires explicit side-effect opt-in and a replay-specific handler whose retained record carries the complete requested range and checkpoint-derived effective start. Planning and execution validate effective starts and exclusive ends against bounded broker offset lookups before using no-reset fetches. Fetch and handler concurrency are independently bounded; handler execution is serial by default and optionally overlaps only across partitions while retaining exact independent progress after failure. Record limits, handler deadlines, cancellation, gap/out-of-range failure, exact per-range incomplete progress, completed-partition pausing, single-run ownership, and bounded retriable shutdown fail closed. Payload-free observers report broker-validated planning, each record outcome, exact resumable aggregate progress, shutdown, and replay broker activity with same-reader reentrancy fencing. The pinned broker fixture proves an executable timestamp-derived plan, broker-validated planning without consuming execution, interruption, external-checkpoint resume, cancellation after real handler admission without settlement, bounded cross-partition handler overlap with sequential partition order, future out-of-range rejection, and rejection before polling after Kafka `DeleteRecords` advances the log start. A separate pinned Apache 4.3.1 fixture keeps log start 0 while compaction removes offset 0 and proves replay returns `ErrReplayOffsetGap` before handler admission with the checkpoint unchanged. A three-node fixture then stops a replication-factor-one leader after `[0,3)` is durable, truncates its active segment tail offline, removes its clean-shutdown marker, and proves Kafka recovery lowers the end offset to 2; replay returns `ErrReplayOffsetOutOfRange` before handler admission with next offset 0 unchanged. A six-node separated-role fixture acknowledges `[0,2)` with one follower outside the ISR, forces that stale broker through unclean election, reconciles one former ISR broker to the truncated leader at end offset 1, and proves the original range fails identically before handler admission. | Kafka does not expose the deletion cause of one absent in-bounds offset, so the public gap category remains deliberately conservative. |
| Inspection and health | Explicit bounded calls expose cluster identity, controller visibility, sorted brokers, exact replica/ISR/offline sets, leader epochs, beginning/end offsets, effective `min.insync.replicas`, cleanup, retention, compaction, segment and unclean-election policy, and classic group lag, member identity, and assignments. Calls derive owned request timeouts and fail closed on incomplete, inconsistent, excessive, unauthorized, or unavailable state. Dependency health, local inspector liveness, and readiness with consecutive-failure/recovery hysteresis are distinct. The pinned single-broker fixture proves cluster, controller, non-default topic policy, offsets, and one active static classic-group assignment. The three-broker Apache fixture proves exact cluster identity, three brokers, RF=3/ISR=3, clean leader failover to ISR=2, and restored ISR=3. | Add KIP-848 group inspection, tiered-storage local-retention configuration, typed multi-target partial results, transaction/producer diagnostics, multi-target partial-failure evidence, and service-runner liveness composition. |
| Security | Verified TLS 1.2 with system roots is the zero value. Plaintext is an explicit development-only policy. Owned, bounded trust-anchor, mTLS, PLAIN, SCRAM-SHA-256/512, and OAUTHBEARER providers support rotation and redacted failures. Pinned Apache 4.3.1 fixtures negotiate exact TLS 1.2/1.3, fail unknown-root and hostname verification, require mTLS, exercise PLAIN and both SCRAM mechanisms only through `SASL_SSL`, and validate RS256 JWTs against a JWKS plus exact issuer and audience. Successful producer, consumer, and inspector roles use package providers; absent certificates, incorrect passwords, and wrong OAuth issuers or audiences fail without returned credential disclosure. An authenticated PLAIN principal with no ACL proves producer authorization classification and unchanged inspector broker-error identity without password disclosure. One live mTLS producer observes a broker-enforced idle disconnect, obtains a separately issued replacement client certificate signed by the same CA from its provider, reconnects, and resumes delivery. A separate overlap-first scenario dynamically replaces the broker listener certificate under a new CA; the same producer reloads both roots, reconnects, removes the retired root, reconnects again, and a new retired-root-only client is rejected. Separate live SCRAM-SHA-256 and SCRAM-SHA-512 producers cross the broker-enforced reauthentication lifetime, refresh their providers after credential replacement, resume delivery, and reject each retired secret on a new connection. The independently versioned MSK IAM adapter uses AWS's supported Go signer with a bounded refreshing SDK v2 credential provider and caps effective token expiry at the signing credential expiry without adding AWS to the root module. | Add broader live expiry/rotation stress and direct MSK Provisioned or Serverless evidence before making those support claims. |
| Hooks and telemetry | An optional root `ObserverPolicy` reports copied, payload-free synchronous producer delivery; consumer record, batch, commit, poll, and group lifecycle; producer and consume-transform-produce transaction lifecycle; producer, consumer, transaction-processor, replay, and inspector shutdown lifecycle; replay plan, record, and run lifecycle; inspector cluster, topic, group, dependency-health, and readiness lifecycle; and every client role's broker activity in registration order. It bounds callback count and a shared cooperative deadline, contains and reports observer errors and panics, and fences same-client mutating and lifecycle reentrancy. The standard-library slog adapter emits only fixed bounded fields with deny-by-default copied Kafka identity allowlists and handler-panic containment. The independently versioned OpenTelemetry adapter maps every current event with the same deny-by-default identity posture and pins messaging semantic conventions 1.43.0 without adding OpenTelemetry to the root module. | Add standalone authentication, retry, complete rebalance timing, and cross-message propagation only through separately reviewed record-header policy. |
| Evidence | Unit, race, exact statement and mutation coverage, root fuzz targets, docs, and microbenchmarks pass. The slog adapter adds exact statement coverage, hostile-input fuzz targets, concurrent observer evidence, and an allocation benchmark. The OpenTelemetry adapter adds exact statement coverage, two hostile-input fuzz targets, race evidence, and an allocation benchmark. The MSK IAM adapter adds deterministic signer, default-chain, expiry, cancellation, panic, redaction, concurrency, fuzz, and allocation checks. One fixture uses one Confluent Local 7.5.0 broker. A second fixture asserts Apache Kafka 4.3.1 at runtime across three combined KRaft broker/controller nodes and proves one broker-process failure/recovery path for inspection, production, and producer transactions, bounded ambiguous non-transactional and transactional delivery after every matching `Produce` response is lost, source-offset preservation for the processor fault, same-transactional-ID fencing, process-termination recovery, eager plus cooperative two-process consume-transform-produce rebalance recovery, broker-enforced transaction expiry, and a committed transaction whose `EndTxn` responses are lost to the producer. A three-node Apache fixture additionally proves fail-closed replay after offline segment-tail truncation and broker log recovery lowers a partition end offset. A six-node separated-role Apache fixture proves fail-closed replay after an unclean election truncates one acknowledged offset. Six isolated Apache 4.3.1 fixtures assert the broker and OpenSSL versions, run Kafka as UID 1000, generate ephemeral credential material, and prove TLS/mTLS, PLAIN, both SCRAM mechanisms, signed-JWT OAUTHBEARER, successful package client roles, bounded authentication failures, authenticated ACL denial, live mTLS client-certificate renewal, live SHA-256 and SHA-512 credential replacement, and deterministic compacted replay-gap handling. A release dry-run from commit `2dc6459` installs `pkg/kafka@v0.0.0` through the local source proxy in a fresh `GOWORK=off` module and lists its public package. The client harness adds separate four-client producer single-record, batch, and bounded asynchronous correctness checks plus three-client keyed and explicit eight-partition correctness, with 720 single-record, 960 batch, 960 asynchronous-window, and 480 multi-partition samples for the equivalent durable producer paths. Four-client consumer record and batch checks verify exact handler order and final broker offsets before 960 manually committed consumer samples covering 9,600 operations and 355,200 records. Policy/raw-franz-go cross-partition checks additionally prove sequential and bounded-parallel handling plus exact offsets across eight partitions before 320 samples covering 3,200 operations and 25,600 records. Four-client direct replay correctness and 640 timed single-use operations cover exact 10/100-record ranges, while four-client topic-inspection correctness and 1,600 timed operations cover identical metadata, beginning/end offsets, durability configuration, and normalized state across one/eight partitions. Thirty cooperative-sticky rebalance samples prove exact one- and two-member assignments, joining-member handling and commit, restored ownership after leave, and both observable stability durations. Ninety verified-TLS 1.3 samples cover 6,000 persistent deliveries and 3,000 complete connection-delivery-shutdown lifecycles for the policy, raw franz-go, and Sarama against pinned Apache Kafka 4.3.1 with OpenSSL 3.5.7. Forty restart samples recover exact three-partition state and report post-readiness latency and allocations; 40 idle samples report heap, goroutine, CPU, and connection state while proving complete connection cleanup. Every capture has a separate exact runtime and input fingerprint. | Add broader live credential rotation stress, public-proxy release resolution, broader compatibility, mTLS and SASL performance, and the remaining controlled-environment matrix. |

Minimum-version evidence separately pins the official Apache Kafka 3.7.2 image
by multi-platform digest, asserts the running version, and executes the producer
plus consume-transform-produce transaction contract across three combined KRaft
nodes. The policy floor is direct operational evidence, not an inference from
franz-go request support or Kafka's protocol history.

## Public boundary violations

The draft does not expose `kgo.Client`, `kgo.Record`, `kgo.Opt`, kadm response
types, or franz-go SASL mechanisms through ordinary public APIs. Security
mechanism translation is internal to the franz-go-backed implementation.

`Publish` remains the error-only compatibility method. `PublishRecord`,
`PublishBatch`, and `PublishAsync` now expose assigned partition, offset, and
timestamp. Broker throttling is request-level metadata and is therefore
reported through `ObservationBrokerThrottle`, not falsely attributed to an
individual delivery result. Missing backend delivery results are classified as
ambiguous failures.

## Reverse dependencies

The repository manifest records three owned reverse dependencies:

- `event-sourcing/adapters/gokafka`;
- event-sourcing Kafka propagation in `adapters/gotelemetry`; and
- `outbox/adapters/gokafka`.

Every breaking pre-v1 correction must compile and pass the applicable adapter
contract and real-broker tests in the same coherent change. Kafka package tests
alone cannot establish completion.

## Baseline evidence

The following local evidence was executed on Darwin arm64 with Go 1.26.5:

- `make check`: format, vet, unit, race, exact statement coverage, five
  10,000-execution fuzz targets, the existing microbenchmark, and documentation
  checks passed;
- integration, mutation, root release gates, auth, chaos, compatibility, and
  reverse-dependent adapter gates were not part of that command; and
- the existing integration fixture is
  `confluentinc/confluent-local:7.5.0@sha256:8e391de42cfcd3498e7317dcf159790f1f1cc3f3ffce900b30d7da23888687fd`.

This baseline proves only the behavior already present in the draft.

The latest recorded package gate killed every viable mutant with 100% test
efficacy and mutator coverage. That result proves the deterministic package
assertions detect the generated mutations; it does not substitute for the
broker, authentication, and compatibility evidence listed above.

The root module now publishes `kafkatest` conformance suites for producer,
consumer, Kafka transaction, replay, inspector, authentication-provider, and
observer seams. The dedicated gate runs the broker-backed suites against the
digest-pinned single-node fixture and the provider/hook suites without a
broker. This closes the missing public-suite surface; it does not extend the
compatibility matrix beyond the exact fixture profile. The conformance run also
confirmed that franz-go v1.21.5 ages delivery from an explicit record timestamp;
the root policy still needs an explicit reviewed contract for historical event
timestamps older than the configured delivery bound.

Current post-baseline integration evidence additionally runs
`apache/kafka:4.3.1` by immutable multi-platform digest, asserts `4.3.1` from
the running container, and exercises three combined KRaft broker/controller
nodes. It proves only the exact RF=3, `min.insync.replicas=2`, clean
leader/ISR, acks-all delivery, and producer-transaction recovery scenario
described above. A separate fixture uses three controller-only plus three
broker-only nodes to prove only the unclean-election replay-truncation scenario
described above. Six isolated combined-mode nodes use secured client listeners
to prove only the listed TLS, mTLS, PLAIN, SCRAM, signed-JWT OAUTHBEARER, and
compacted replay-gap scenarios; they do not expand the broker-version or
managed-service claim.
