# Compatibility and execution matrix

This matrix separates pinned implementation inputs from support claims. It was
recorded through 2026-08-10. Upstream protocol support is not package evidence.
The [specification decision register](specification-decisions.md) records the
package interpretations that govern this matrix.

The independently versioned `kafkaservice` module is additive and pre-v1.
Existing direct producer and consumer construction remains supported. The
adapter uses the public Kafka record, handler, health, run, and shutdown
contracts and does not change their wire or settlement semantics.

## Pinned inputs

| Input | Exact version or identity | Verification |
| --- | --- | --- |
| Go toolchain and module language | Go 1.26.5, `go 1.26.5` | `go version`, `go env`, and `go.mod` |
| Host used for baseline | Darwin arm64, Apple M4 Max | Go environment and benchmark output |
| Container runtime | Docker Desktop engine 29.6.2, API 1.55 | Benchmark environment capture on 2026-07-30 |
| franz-go | v1.21.5, tag target `1ba5fd24f949a335dbc7caaef1d6037e132ef23e` | Go module proxy plus upstream tag; latest stable rechecked on 2026-08-11 |
| kadm | v1.18.0, tag target `a7255a3f2bc7247e70a15b18080cc4e5cd1e42d6` | Go module proxy plus upstream tag; latest stable rechecked on 2026-08-11 |
| Comparison clients | kafka-go v0.4.51; IBM/Sarama v1.60.1 | Upstream releases and Go module proxy rechecked on 2026-08-11; isolated non-releasable benchmark module |
| testcontainers-go core and Kafka module | v0.43.0 | Go module proxy |
| Testcontainers resource reaper | `testcontainers/ryuk:0.14.0`, digest `sha256:7c1a8a9a47c780ed0f983770a662f80deb115d95cce3e2daa3d12115b8cd28f0` | The integration entry point rejects an upstream tag change or configured registry substitution before container creation, resolves or pulls the immutable digest, binds testcontainers-go's required upstream tag to that exact image ID, starts the reaper, and rejects a running container whose image ID differs. This preserves upstream reaper recognition and cleanup without trusting the mutable tag. |
| Existing broker fixture | Confluent Local 7.5.0 digest `sha256:8e391de42cfcd3498e7317dcf159790f1f1cc3f3ffce900b30d7da23888687fd` | Source-pinned single-node integration test and benchmark harness both reject a runtime version other than `7.5.0-ccs` |
| Minimum Apache Kafka fixture | `apache/kafka:3.7.2`, multi-platform index `sha256:8bd63e1bd445e5e19427a4bdbcc3d23bf6efd774b058a41b36ba87fda7623e34` | Source-pinned three-node combined KRaft fixture reports exactly `3.7.2` and proves producer plus consume-transform-produce transaction commit, abort, isolation, offset atomicity, redelivery, same-transactional-ID fencing, broker-enforced expiry, committed `EndTxn` response loss, bounded ambiguous transactional `Produce` response loss, process-termination recovery, and eager plus cooperative transaction-processor rebalance recovery, plus producer commit and abort at ISR two during one follower outage and after ISR-three recovery |
| Current Apache Kafka fixture | `apache/kafka:4.3.1`, multi-platform index `sha256:77e3df9054047a88b520d0cc46e16696d3b22022e1d580aeccd2632df6532837` | Source-pinned three-node combined KRaft and isolated secured single-node fixtures; every fixture reports exactly `4.3.1`; the combined fixture directly proves KIP-848 consumer-protocol inspection; the secured fixtures report OpenSSL 3.5.7 and run the broker as image UID 1000; arm64 manifest `sha256:c2b5172ab20d66381ec1729796a410fd611135821994526d4d42d2f256054af3` |
| Apache Kafka tiered-storage test plugin | `org.apache.kafka:kafka-storage:4.3.1:test`, 638,839 bytes, SHA-256 `f3236936854c13815ffaa7f35a3f2d6861c125616cba5395b538b8680dded886`, Apache-2.0 | The fixture downloads the official Maven Central test JAR through a bounded request and rejects any size or digest mismatch before starting Kafka; it exercises only Kafka's test-only `LocalTieredStorage` implementation |
| Mutation tool | patched Gremlins v0.6.0 | The current-tree module gate requires and records exact 100% efficacy and mutator coverage for the root package and bundled slog adapter; transient mutant counts remain in its attributable evidence artifact rather than this version matrix |
| Lint/static analysis | golangci-lint v2.12.2, Staticcheck v0.7.0, NilAway `9fd1b8d7bac8` | Repository tool pins |
| Security/release tools | govulncheck v1.6.0, Gitleaks v8.30.1, go-licenses v2.0.1, CycloneDX v1.10.0 | Repository tool pins |
| OpenTelemetry semantic conventions and Go API | Semantic conventions 1.44.0 at `e10a930844c6951757a43b849d364f7d056ac32b`; Go v1.44.0 | Independently versioned adapter provenance and module pins |
| MSK IAM Go signer and AWS SDK for Go v2 | Signer v1.0.4 at `53637de1b411b2a2c8b2ccb8f103fc1d6b761c07`; SDK v1.43.0 and config v1.32.31 at `4fef3455fe2dcb5ea3de4e9fbacf889b84c8a255` | Upstream tags, Go module proxy, and adapter provenance |

Apache Kafka 4.3.1 was the latest supported Apache release found at execution
time. Kafka 3.7.2 is the minimum package-supported broker release because it is
the earliest official Apache Kafka image line directly exercised here; this is
an operational policy floor, not an upstream protocol limit. The 3.7.2 fixture
currently proves only plaintext combined-KRaft producer and
consume-transform-produce transactions. The 4.3.1 fixtures establish the
broader scenarios below. Separate 4.3.1 nodes establish only the listed TLS,
mTLS, PLAIN, SCRAM, and signed-JWT OAUTHBEARER paths. These fixtures do not
establish the complete support matrix.

Broker lifecycle support is a separate production prerequisite. As of
2026-08-11, Apache Kafka lists 4.3.1, 4.2.1, and 4.1.2 as supported releases and
lists 3.7.2 as archived. The 3.7.2 result therefore proves the package's tested
client-compatibility floor; it does not imply ongoing broker security
maintenance or make 3.7.2 a recommended production deployment. Operators must
select a broker line still supported by Apache Kafka or their managed-service
vendor and must apply that provider's security and bug-fix releases. A package
compatibility result never extends a broker's support lifetime.

The zero `ProtocolPolicy` negotiates request versions with each connection.
`MinimumVersion` is only a request downgrade floor recognized by franz-go; it
does not prove or constrain the broker release and does not change this matrix.
The transaction processor is stricter: its empty policy becomes a Kafka 2.5
request floor and lower explicit values are rejected for KIP-447 safety. That
floor still does not establish operational support for an untested broker.

## Current support status

| Dimension | Planned matrix | Current status |
| --- | --- | --- |
| Go | Minimum and current repository-supported releases | Go 1.26.5 is exercised locally on Darwin arm64 and by the root Linux amd64 quality job. A Kafka-specific required test job uses the same pinned toolchain on Linux arm64 whenever Kafka gate inputs are selected and during full scheduled or manual runs. |
| Apache Kafka | Minimum 3.7.2 and current 4.3.1, KRaft, three brokers | The minimum 3.7.2 fixture runs three combined KRaft broker/controller nodes, asserts the exact runtime, and proves producer plus consume-transform-produce transaction commit, abort, read isolation, source-offset atomicity, redelivery, same-transactional-ID producer fencing, the known fenced commit outcome returned after broker-enforced transaction expiry, committed `EndTxn` response loss, bounded ambiguous transactional `Produce` response loss, process-termination recovery, and eager plus cooperative transaction-processor rebalance recovery at RF=3 with `min.insync.replicas=2`. It additionally proves producer commit and abort isolation while one follower is unavailable at ISR two, then full ISR-three recovery. Current 4.3.1 runs the same topology with exact runtime assertion and additionally proves clean leader election, continued acks-all delivery at ISR=2, endpoint and ISR recovery, bounded ambiguous non-transactional delivery after every matching `Produce` response is lost, committed/aborted transactions before and after broker recovery, same-transactional-ID fencing, the ambiguous unknown commit outcome returned after broker-enforced expiry, committed `EndTxn` response loss, process-termination recovery, and eager plus cooperative rebalance recovery. A separate 4.3.1 topology with three controller-only and three broker-only nodes proves one exact unclean-election truncation and replay scenario. Isolated 4.3.1 nodes prove the secured client-listener paths below. Supported fetch memory reclamation assumes Kafka record batches (magic 2). Legacy compressed message sets (magic 0 or 1) retain hard response and decoded-batch bounds but lack reclaimable active-buffer accounting in franz-go; they are unverified and unsupported. An isolated 4.3.1 combined KRaft node additionally loads Kafka's exact checksum-pinned test-only `LocalTieredStorage` plugin, copies closed user-topic segments, evicts the local offset-zero segment, preserves broker beginning offset zero, and serves the package's exact `[0,1)` replay from the remote copy. Broader separated-role operations, repeated rebalance churn, broader credential rotation, production remote-storage plugins, and managed-service evidence remain unverified. |
| Confluent Platform/Local | Only explicitly exercised versions | One single-node 7.5.0 compatibility test with exact `7.5.0-ccs` runtime assertion; not a production support claim |
| Amazon MSK Provisioned | Selected Kafka versions, TLS/mTLS/SCRAM/IAM | Unverified |
| Amazon MSK Serverless | TLS and IAM with documented service limits | Unverified |
| Redpanda, Confluent Cloud, Event Hubs, other compatible services | Add only after direct testing | Unverified and unsupported |
| TLS and mTLS | TLS 1.2/1.3, hostname/root failures, rotation | The pinned Apache 4.3.1 broker negotiates exact TLS 1.2 and 1.3, rejects an unknown root and wrong hostname, and requires a client certificate. Provider-backed mTLS producer delivery, consumer settlement, and inspector health pass; the same broker rejects a client without a certificate. Three independent producers cross three broker-enforced idle-disconnect cycles, obtain each successive client certificate signed by the same CA from every provider, reconnect, and preserve every acknowledged record in exact broker order. A separate overlap-first scenario dynamically replaces the broker listener keystore with a certificate under a new CA; an existing producer loads both roots for a bounded reconnect, then loads only the new root for another reconnect, while a new client trusting only the retired root is rejected. |
| SASL/PLAIN | Verified TLS only | The pinned Apache 4.3.1 `SASL_SSL` listener accepts provider-backed production and rejects an incorrect password. With KRaft `StandardAuthorizer` enabled, a separately authenticated principal with no matching ACL receives a classifiable topic-authorization delivery failure and unchanged inspector authorization identity without password disclosure. A least-privilege transactional producer with topic write access receives exact `TRANSACTIONAL_ID_AUTHORIZATION_FAILED` identity under stable redacted package classification, commits one read-committed record after its transactional-ID ACL is granted, then receives the same denial after that ACL is revoked from the active client without exposing the denied record. Three independent producers recover after the default broker verifier is replaced through a bounded broker restart, invoke every provider with the replacement password, preserve every acknowledged record, and reject the retired password. Because Kafka 4.3.1 does not reload the exercised default PLAIN JAAS verifier in place, a separate three-broker fixture uses overlapping old and new principals: one provider-backed producer adopts the new identity, preserves exact acks-all delivery across three partitions while each RF=3 broker restarts separately at `min.insync.replicas=2`, waits for ISR=3 between restarts, and proves every broker accepts the new credential before all three reject the retired one. This proves the reviewed overlap-first rollout, not atomic in-place verifier replacement. |
| SCRAM-SHA-256/512 | Verified TLS only | The pinned Apache 4.3.1 KRaft metadata log is initialized with independently generated SHA-256 and SHA-512 credentials. Provider-backed production succeeds for each mechanism over `SASL_SSL`, incorrect credentials fail, SCRAM-SHA-512 consumption settles the full record set, and SCRAM-SHA-256 inspection succeeds. Three independent producers per mechanism cross the broker-enforced reauthentication lifetime through three successive live credential replacements, invoke every provider again, preserve every acknowledged record, and reject every retired credential on a new connection. |
| OAUTHBEARER | Refreshing provider over verified TLS | The pinned Apache 4.3.1 `SASL_SSL` listener uses its production JWT validator with an RS256 JWKS plus exact issuer and audience checks. Provider-backed production and consumption succeed; correctly signed tokens for the wrong issuer or audience fail without appearing in the returned error. Three independent producers cross three broker-enforced reauthentication cycles through successive signed-JWT replacements, invoke every provider again, and preserve every acknowledged record; still-valid retired tokens are not claimed rejected before signed expiry. A separate fixture verifies the broker's HTTPS JWKS peer, observes periodic refresh before use of a new `kid`, preserves acknowledged delivery after provider reauthentication under the overlapping new key, removes the old key, and requires exact redacted `SASL_AUTHENTICATION_FAILED` identity when a new client presents a still-valid token signed by the retired key. A separate caller-owned provider posts OAuth 2.0 `client_credentials` to a verified HTTPS endpoint, rejects an untrusted peer, propagates bounded cancellation, refreshes its signed token during broker-enforced reauthentication, and preserves every acknowledged record. Equivalent persistent delivery and complete client-lifecycle performance is captured for the policy, raw franz-go, and Sarama. The local endpoint fixture does not establish compatibility with a specific identity provider. |
| MSK IAM | Optional AWS signer adapter | The independently versioned adapter uses the supported Go signer, refreshing SDK v2 default chain or explicit provider, bounded cancellation and refresh, effective expiry capped by signing credentials, and redacted failures. Local contract, race, fuzz, and allocation evidence exists; no Provisioned or Serverless broker has been exercised, so operational compatibility remains unverified. |
| Producer | Single, batch, async, ordering, failure, shutdown | Policy APIs and deterministic tests exist. The pinned single-broker fixture proves ordered batch and asynchronous delivery metadata, exact broker-visible values, graceful shutdown draining an admitted asynchronous record, rejection after shutdown, and successful delivery under a 1 KiB/s client-ID quota with a positive post-response throttle event. Throttling remains request-level because Kafka produce responses can cover many records. The Apache fixture proves a two-topic batch whose first input is acknowledged and persisted while the second is rejected by a topic-level `max.message.bytes` policy; results remain in input order with an `ErrorOversized` delivery only on the rejected record. It also proves explicit-partition synchronous delivery before failure, after clean leader failover at ISR=2, after ISR=3 recovery, and after a fault forwards the record while dropping every matching `Produce` response. The response-loss case returns within the configured delivery plus retry-backoff bound, reports an ambiguous timeout, and a separate consumer observes exactly one broker record. |
| Consumer group | Classic cooperative/eager and reviewed next-generation protocol | Explicit cooperative-sticky, eager-sticky, migration, static-membership, rack, bounded cross-partition handling, partition pause/resume, and per-record plus whole-batch failure policy exists. The single-node fixture proves eager membership, same-ID static restart, duplicate-live-instance fencing into a terminal package state, pause/resume, concurrent independent-partition handling with sequential partition order, per-record retry/dead-letter publication followed by source settlement, redelivery after failed publication, and whole-partition-batch retry-topic publication with exact source coordinates and batch positions before source settlement. It also proves two eager-join handler-overlap outcomes: cancellation returns `ErrConsumerRebalance`, leaves the offset unsettled, and redelivers the source record; draining finishes and commits the active handler before releasing the blocked rebalance, after which the joining member receives only the next offset. The same pinned broker accepts one highly compressible three-record Kafka batch whose encoded form is within broker policy but whose decoded form exceeds package policy; consumer-group, consume-transform-produce, and direct replay clients all return the stable oversized sentinel before handler admission and preserve the source offset or replay checkpoint. Unit evidence additionally proves partial target results leave the complete source batch unsettled and remain input-ordered for reconciliation. A separate three-broker Apache Kafka 4.3.1 fixture uses three operating-system child processes to prove an eager-only plus migration member negotiates `sticky`, the migration member retains both partitions after the eager member leaves, a migration plus cooperative-only member negotiates `cooperative-sticky`, and the cooperative member retains both partitions after the migration member leaves. Every stable state verifies exact client identities and one-copy ownership of both partitions. With one rack per broker and Kafka's rack-aware replica selector enabled, a separate consumer process configured for a non-leader replica's rack handles and commits a source record after its single in-flight fetch completes on that follower. Another two-process eager fixture co-locates two partition leaders, uses bounded minimum fetch bytes to admit both records into one poll, and proves cancellation commits neither active partition before both offset-zero records are redelivered; drain commits both partitions at offset one before ownership transfer and replacement handling starts from offset one. The current three-broker runtime additionally proves a cooperative join revokes exactly one partition only after the original member drains and commits both in-flight partitions; retained and replacement members then process disjoint offset-one records. Minimum Kafka 3.7.2 and current 4.3.1 each complete five additional cooperative join/leave cycles with exact one-copy ownership in every stable two-member and survivor state while one new record per partition settles during each cycle and both committed offsets advance from one through five. Administrative removal of an active static member proves Kafka rejects its post-handler stale-generation commit with `UNKNOWN_MEMBER_ID`, the package reports and clears the lost assignment, and a replacement redelivers offset zero. A separate process-suspension scenario proves the broker's six-second session timeout transfers that same in-flight offset to a replacement, which settles it before the resumed stale member returns `UNKNOWN_MEMBER_ID` and reports ownership loss. |
| Transactions | Produce and consume-transform-produce | Producer-only and source-offset consume-transform-produce commit/abort isolation are exercised against the pinned single-node fixture. A separate pinned Apache Kafka 3.7.2 three-node fixture proves the minimum supported broker performs producer commit/abort isolation and consume-transform-produce commit, source-offset/output atomicity, abort invisibility, source redelivery, successful retry, same-transactional-ID producer fencing, the known fenced commit outcome returned after broker-enforced expiry, committed `EndTxn` response loss, bounded ambiguous transactional `Produce` response loss for producer-only and consume-transform-produce clients, process-termination recovery, and eager plus cooperative rebalance recovery without committing the interrupted transaction at RF=3 with ISR=3, then repeats producer commit and abort isolation while one follower is unavailable at ISR two and after ISR-three recovery. Current 4.3.1 proves producer isolation before and after broker recovery and atomic processing while one broker is unavailable at ISR two. It additionally proves same-transactional-ID fencing, the ambiguous unknown commit outcome returned after coordinator-enforced expiry, committed `EndTxn` response loss, bounded ambiguous transactional `Produce` response loss, process-termination recovery, and eager plus cooperative rebalance recovery. Minimum 3.7.2 and current 4.3.1 each complete five repeated cooperative two-member processing and survivor-reacquisition cycles. A retained rebalance state returns `ErrTransactionNotCommitted`; bounded application retry redelivers the source, both source offsets advance exactly once per cycle, and ten outputs remain read-committed visible. |
| Replay | Offset/timestamp planning, exact ranges, gaps, resume | Local and broker-validated checkpoint-aware dry-run plans, exact-partition timestamp-window plans, explicit side-effect opt-in, replay-specific range and resume metadata, broker-boundary preflight, exact no-reset ranges, independently bounded fetch and cross-partition handler concurrency, resumable per-range progress, cancellation, bounded shutdown, and payload-free plan, record, exact-progress, shutdown, and broker observations are implemented. The single-node fixture proves an executable timestamp-derived plan, broker-validated planning without consuming execution, interrupted replay, external-checkpoint resume, cancellation after real handler admission without settlement, bounded cross-partition handler overlap with sequential per-partition order, future out-of-range rejection, and fail-closed rejection after Kafka `DeleteRecords` advances the log start. A separate pinned Apache 4.3.1 fixture keeps the log start at offset 0 while compaction removes that exact offset, then proves `[0,1)` fails as `ErrReplayOffsetGap` without handler admission and with an unchanged checkpoint. A separate three-node Apache fixture stops the sole replica leader after `[0,3)` is durable, truncates its active segment tail offline, removes the clean-shutdown marker, and proves broker recovery lowers the end offset to 2; replay rejects the original range as `ErrReplayOffsetOutOfRange` before handler admission with next offset 0 unchanged. A checksum-pinned test-only `LocalTieredStorage` fixture additionally proves exact `[0,1)` replay after Kafka evicts the local offset-zero segment while preserving broker beginning offset zero. A six-node separated-role fixture then acknowledges `[0,2)` at ISR two, forces the stale replica through unclean election, reconciles a former ISR replica to the truncated leader at end offset 1, and proves replay rejects the original range before handler admission with next offset 0 unchanged. |
| Inspection/health | Cluster/topic/group/durability and separated health signals | Single-broker cluster/controller, topic replica/ISR/offline, beginning/end offset, effective `min.insync.replicas`, non-default cleanup/topic-wide retention/local retention/compaction/segment/unclean-election policy, classic-group lag/member assignment, input-ordered partial topic success beside a missing target, Kafka's successful `Dead` state for an unknown classic group, dependency health, local liveness, and readiness-hysteresis evidence. Producer and transaction-processor diagnostics expose payload-free local admission, lifecycle, transaction, shutdown, redacted fatal-category, forced-client-termination, and bounded buffered-output state without claiming broker or coordinator health; public conformance exercises active transactions, completed processing, and producer shutdown. A pinned three-broker Apache Kafka 4.3.1 fixture additionally proves KIP-848 group, assignment, and member epochs; server assignor; static-instance, rack, client, explicit-topic and broker-side regex subscription state; reconciled current and target assignments; stable committed offsets; log bounds; and lag. The local-retention fixture proves explicit finite `local.retention.ms` and `local.retention.bytes` values plus disabled remote-storage/copy flags; it does not enable or exercise a remote-storage implementation. The secured Apache fixture proves that an authenticated PLAIN principal with `DESCRIBE` permission on exactly one group and its topic retains that authorized `Empty` group and committed offset beside a separate input-ordered `GroupAuthorizationFailed` result with stable package classification and credential redaction. The Apache fixture additionally proves exact three-broker cluster identity, RF=3/ISR=3, clean leader failover at ISR=2, and ISR=3 recovery. The independently versioned `kafkaservice` local contract proves bounded producer startup and dependency readiness, optional consumer readiness, callback panic containment, admission fencing, drain-before-shutdown ordering, and retryable cleanup. Its immutable-digest Apache Kafka 4.3.1 fixture additionally proves concrete producer and consumer composition, pre-cancellation settlement, admitted-handler join before close, explicit in-flight cancellation redelivery to a replacement member, and post-stop fencing. The isolated test-only `LocalTieredStorage` fixture additionally proves visible enabled/copy/local-retention policy and stable broker offsets after observable remote copy plus local base-segment eviction. Production remote-storage plugins and managed tiered-storage services remain unverified. |
| Observability | Producer, consumer, rebalance, transaction, replay, inspection, and lifecycle events plus optional adapters | Root payload-free producer delivery, nontransactional consumer record/batch/commit/poll, retry and group lifecycle, including the bounded local interval from franz-go's blocked-rebalance signal through poll-gate release, producer and consume-transform-produce transaction lifecycle, producer/consumer/transaction-processor/replay/inspector shutdown, inspector cluster/topic/group/dependency/readiness, and every client role's broker events are deterministically tested. Broker-connect observations identify the configured bounded SASL method; successful events cover dial, API-version negotiation, and authentication initialization without inventing a separate upstream-unsupported authentication event or latency. The pinned SASL broker fixture proves PLAIN, SCRAM-SHA-256, and SCRAM-SHA-512 successful connections report their configured method. The single-broker and three-broker Apache fixtures prove blocked-poll wait completion around real group joins without claiming complete broker-wide or multi-phase cooperative duration. The single-broker fixture also proves producer and consume-transform-produce begin/commit/abort events plus transaction-processor broker requests. The standard-library slog adapter emits fixed bounded scalar fields, denies every Kafka identity by default, contains handler panics, and has exact local statement, fuzz, race, concurrency, and allocation evidence. The independently versioned OpenTelemetry adapter maps every current event, pins development-status messaging semantic conventions 1.44.0, defaults every identity attribute off, and has exact local statement, fuzz, race, concurrency, and allocation evidence. Its completion seam implements producer-send, receive, process, and settle spans but omits the newly recommended cluster ID and the create/client-send link model because it has neither authoritative cluster metadata nor a pre-publication creation context; no hidden lookup or inference is performed. Its separate fixed W3C record-header policy validates Kafka limits, owns injected producer bytes, borrows consumed records synchronously, rejects duplicate trace fields, excludes baggage, and reads no global propagator. A pinned Apache Kafka 4.3.1 fixture proves injected headers survive root-producer publication and root-consumer fetch, extract as the exact remote W3C context, and settle the source offset. Complete broker-wide rebalance timing remains unverified. |
| Performance | Equivalent producer, consumer, transaction, replay, inspection, reconnect, resource, and policy-overhead workloads | The isolated client harness has separate real-broker producer single-record, batch, bounded asynchronous, eight-partition, producer-transaction, consume-transform-produce, direct replay, and read-only topic-inspection correctness evidence. Timed producer evidence covers equivalent idempotent all-ISR synchronous single-record, 10/100-record batch, bounded 10/100-record asynchronous-window, balanced Murmur2-keyed plus explicitly assigned 80-record eight-partition, and one/ten-record transaction contracts for the package policy, raw franz-go, and Sarama. Kafka-go is an unranked ordinary-producer correctness control because v0.4.51 cannot match idempotence and is excluded from transactional production because its writer exposes no Kafka transaction. Automatic unkeyed multi-partition production is excluded because franz-go and Sarama do not expose equivalent distribution strategies. Separate consumer evidence proves exact record and 10/100-record batch handling followed by broker-verified manual commits for the policy, raw franz-go, kafka-go, and Sarama. A further policy/raw-franz-go workload proves sequential and bounded-parallel handling, exact per-partition order, and broker-verified commits across eight partitions; kafka-go and Sarama are excluded because their group APIs do not expose an equivalent bounded multi-partition poll-and-commit cycle. A policy/raw-franz-go consume-transform-produce capture proves exact transformed outputs, source-offset advancement, abort invisibility, and source redelivery across one/ten-record transactions. Four-client direct replay evidence measures complete single-use lifecycle plus exact broker-bound ranges for 10/100 records, while four-client topic inspection measures the same metadata, offset, configuration, and normalized durability state across one/eight partitions. Thirty cooperative-sticky rebalance samples compare construction through committed handling and exact two-member stability plus close through restored one-member stability for the policy, raw franz-go, and Sarama; kafka-go is excluded because it cannot match that protocol. Ninety verified-TLS 1.3 samples cover 6,000 persistent deliveries and 3,000 complete client connection-delivery-shutdown lifecycles for the policy, raw franz-go, and Sarama against pinned Apache Kafka 4.3.1; kafka-go is excluded because it cannot match idempotence. Forty fixed-endpoint restart samples measure exact post-readiness inspection recovery and allocations, while 40 idle samples report heap, object, goroutine, Go runtime CPU, and connection state with complete observed connection cleanup. A refreshed authenticated-producer capture adds 3,000 warmed deliveries and 1,500 complete connection lifecycles through required mTLS, SASL/PLAIN, SCRAM-SHA-256, SCRAM-SHA-512, and signed-JWT OAUTHBEARER over TLS 1.3 for the policy, raw franz-go, and Sarama, with exact correctness, race, runtime, input-fingerprint, raw-sample, and allocation evidence. Every capture retains raw results, a workload-specific environment fingerprint, and variance-preserving analysis. External identity-provider performance, the previous released version, and controlled-runner results remain unverified. |
| Operating systems/architectures | Linux amd64/arm64 plus repository-supported developer platforms | Darwin arm64 is exercised locally. The normal Kafka module contract runs on Ubuntu 24.04 amd64, and a separate required Kafka test contract runs on the `ubuntu-24.04-arm` GitHub-hosted runner when Kafka gate inputs are selected and during full scheduled or manual runs. Each job uploads attributable Kafka evidence. |

## Release evidence

On 2026-07-29, commit `2dc6459` passed the repository release dry-run for
`pkg/kafka`. After module-local tidy, test, and API-compatibility gates, the
release tool created a fresh temporary module with `GOWORK=off`, resolved
`github.com/faustbrian/golib/pkg/kafka@v0.0.0` through the local source proxy,
and listed the public package successfully. This proves the committed module is
independently consumable from the repository's release artifact. It does not
prove a published tag or public Go module proxy resolution.

## Primary sources

Design and implementation are checked against:

- [Apache Kafka documentation](https://kafka.apache.org/documentation/) and
  [supported downloads](https://kafka.apache.org/community/downloads/), plus
  the official [Kafka 3.7 Docker image
  documentation](https://kafka.apache.org/37/getting-started/docker/) and
  the broker-compatible rules in Kafka's
  [`Topic` source](https://github.com/apache/kafka/blob/4.3.1/clients/src/main/java/org/apache/kafka/common/internals/Topic.java);
- [Apache Kafka producer configuration](https://kafka.apache.org/43/configuration/producer-configs/),
  [broker configuration](https://kafka.apache.org/43/configuration/broker-configs/),
  [topic configuration](https://kafka.apache.org/43/configuration/topic-configs/),
  [consumer configuration](https://kafka.apache.org/43/configuration/consumer-configs/),
  [consumer rebalance protocol](https://kafka.apache.org/43/operations/consumer-rebalance-protocol/),
  [transaction protocol](https://kafka.apache.org/43/operations/transaction-protocol/),
  [tiered-storage operations](https://kafka.apache.org/43/operations/tiered-storage/),
  [tiered-storage configuration](https://kafka.apache.org/43/configuration/tiered-storage-configs/),
  and [client quota operations](https://kafka.apache.org/43/operations/basic-kafka-operations/#setting-quotas);
- [Apache Kafka TLS](https://kafka.apache.org/43/security/encryption-and-authentication-using-ssl/),
  [SASL authentication](https://kafka.apache.org/43/security/authentication-using-sasl/),
  [authorization and ACLs](https://kafka.apache.org/43/security/authorization-and-acls/),
  [listener configuration](https://kafka.apache.org/43/security/listener-configuration/),
  and the OAUTHBEARER
  [URL allowlist system property](https://kafka.apache.org/43/configuration/system-properties/);
- [KIP-98](https://cwiki.apache.org/confluence/display/KAFKA/KIP-98%2B-%2BExactly%2BOnce%2BDelivery%2Band%2BTransactional%2BMessaging)
  defines the single-active-producer guarantee for one transactional ID and
  fencing of an older generation when its replacement starts;
- [KIP-219 quota communication](https://cwiki.apache.org/confluence/pages/viewpage.action?pageId=75962388)
  and [KIP-546 client quota APIs](https://cwiki.apache.org/confluence/display/KAFKA/KIP-546%3A+Add+Client+Quota+APIs+to+the+Admin+Client);
- [franz-go v1.21.5 package documentation](https://pkg.go.dev/github.com/twmb/franz-go/pkg/kgo@v1.21.5),
  including its tag-pinned
  [`RecordDeliveryTimeout`](https://github.com/twmb/franz-go/blob/v1.21.5/pkg/kgo/config.go)
  and idempotent in-flight cancellation contracts; the root policy does not
  select `RecordDeliveryTimeout` because v1.21.5 ages it from Kafka event time
  and instead bounds delivery from package admission, plus the tag-pinned
  [`ProduceSync`](https://github.com/twmb/franz-go/blob/v1.21.5/pkg/kgo/producer.go)
  callback aggregation used to verify cross-partition completion ordering,
  [`BlockRebalanceOnPoll`](https://pkg.go.dev/github.com/twmb/franz-go/pkg/kgo@v1.21.5#BlockRebalanceOnPoll),
  and
  [`OnPartitionsCallbackBlocked`](https://pkg.go.dev/github.com/twmb/franz-go/pkg/kgo@v1.21.5#OnPartitionsCallbackBlocked),
  whose goroutine-delivered signal identifies only a callback waiting on the
  application-controlled poll gate;
- the tag-pinned [kafka-go v0.4.51](https://github.com/segmentio/kafka-go/tree/v0.4.51)
  writer source and [IBM/Sarama v1.60.1](https://github.com/IBM/sarama/tree/v1.60.1)
  producer configuration used to establish the benchmark capability boundary;
- [Amazon MSK documentation](https://docs.aws.amazon.com/msk/), including
  [IAM client configuration](https://docs.aws.amazon.com/msk/latest/developerguide/configure-clients-for-iam-access-control.html);
- [OpenTelemetry Kafka semantic conventions](https://opentelemetry.io/docs/specs/semconv/messaging/kafka/);
- [RFC 7517](https://www.rfc-editor.org/rfc/rfc7517) for JWKS and
  [RFC 7519](https://www.rfc-editor.org/rfc/rfc7519) for signed JWT claims; and
- [Go 1.26 release documentation](https://go.dev/doc/go1.26), the Go memory
  model, `context`, `crypto/tls`, fuzzing, race detector, and module docs.

The exact source revision or version used for an implemented capability must be
recorded beside its executable evidence, not inferred later from `HEAD`.
