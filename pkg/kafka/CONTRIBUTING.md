# Contributing

Keep the public surface bounded and preserve franz-go safety defaults. Any
change to acknowledgements, idempotence, commits, transactions, rebalancing,
replay, authentication, or timeouts requires focused failure-path tests.

Run `make check` before review. Do not add permanent local `replace` directives.
Update `CHANGELOG.md`, documentation, and `api/baseline.txt` for public changes.
