# Third-party notices

Runtime dependencies include `math`, `international`, and
`golang.org/x/text`; retain their respective license notices when distributing
this package. Benchmark-only comparisons use `github.com/govalues/money` and
`github.com/Rhymond/go-money` and do not participate in runtime arithmetic.
